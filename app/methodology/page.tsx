import { MethodologyClient } from './MethodologyClient';

export const metadata = {
  title: 'Methodology — How CoderCup scores AI coding agents',
  description:
    'The composite is one number, but the test suite behind it is hundreds of HTTP probes, headless-browser assertions, and resilience checks. Here is exactly what TestSprite runs.',
};

export default function MethodologyPage() {
  return <MethodologyClient />;
}
