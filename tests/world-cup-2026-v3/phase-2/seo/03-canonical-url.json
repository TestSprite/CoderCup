{
  "type": "frontend",
  "name": "Match page exposes a canonical link tag matching the URL",
  "description": "When a visitor opens a match permalink, the document head exposes a canonical link tag whose href matches the requested URL path.",
  "priority": "p2",
  "planSteps": [
    { "type": "action", "description": "Navigate to /api/matches" },
    { "type": "action", "description": "Read the first match id from the response" },
    { "type": "action", "description": "Navigate to /match/<first-id> using the captured id" },
    { "type": "assertion", "description": "Verify the document head exposes a canonical link tag whose href ends with the match permalink path" }
  ]
}
