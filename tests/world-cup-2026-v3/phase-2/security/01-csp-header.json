{
  "type": "frontend",
  "name": "Match page response includes a Content-Security-Policy header",
  "description": "When a visitor opens a match permalink, the HTTP response carries a Content-Security-Policy header.",
  "priority": "p1",
  "planSteps": [
    { "type": "action", "description": "Navigate to /api/matches" },
    { "type": "action", "description": "Read the first match id from the response" },
    { "type": "action", "description": "Navigate to /match/<first-id> using the captured id" },
    { "type": "action", "description": "Inspect the network response headers for the document" },
    { "type": "assertion", "description": "Verify the document response includes a Content-Security-Policy header with a non-empty value" }
  ]
}
