{
  "type": "frontend",
  "name": "Match detail page shows kickoff time",
  "description": "When a visitor opens a match permalink, the page surfaces the kickoff time for the fixture.",
  "priority": "p0",
  "planSteps": [
    { "type": "action", "description": "Navigate to /api/matches" },
    { "type": "action", "description": "Read the first match id from the response" },
    { "type": "action", "description": "Navigate to /match/<first-id> using the captured id" },
    { "type": "assertion", "description": "Verify the match detail page renders the fixture kickoff time as a human-readable timestamp" }
  ]
}
