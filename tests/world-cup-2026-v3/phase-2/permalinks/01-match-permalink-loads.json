{
  "type": "frontend",
  "name": "Sample match permalink loads with team names visible",
  "description": "When a visitor opens a sampled match permalink, the page loads successfully and renders both team names as readable text.",
  "priority": "p0",
  "planSteps": [
    { "type": "action", "description": "Navigate to /api/matches" },
    { "type": "action", "description": "Read the first match id from the response" },
    { "type": "action", "description": "Navigate to /match/<first-id> using the captured id" },
    { "type": "assertion", "description": "Verify the match detail page loads successfully and renders both competing team names as readable text" }
  ]
}
