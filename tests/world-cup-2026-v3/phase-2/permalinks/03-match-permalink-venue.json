{
  "type": "frontend",
  "name": "Match detail page shows venue host city",
  "description": "When a visitor opens a match permalink, the page surfaces the venue with at least the host city.",
  "priority": "p1",
  "planSteps": [
    { "type": "action", "description": "Navigate to /api/matches" },
    { "type": "action", "description": "Read the first match id from the response" },
    { "type": "action", "description": "Navigate to /match/<first-id> using the captured id" },
    { "type": "assertion", "description": "Verify the match detail page renders a venue location containing the host city name" }
  ]
}
