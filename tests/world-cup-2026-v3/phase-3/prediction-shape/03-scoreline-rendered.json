{
  "type": "frontend",
  "name": "Prediction block renders a concrete scoreline",
  "description": "When a visitor opens a match permalink, the prediction block renders a concrete scoreline formatted as two numbers separated by a dash or em-dash.",
  "priority": "p0",
  "planSteps": [
    { "type": "action", "description": "Navigate to /api/matches" },
    { "type": "action", "description": "Read the first match id from the response" },
    { "type": "action", "description": "Navigate to /match/<first-id> using the captured id" },
    { "type": "assertion", "description": "Verify the prediction block renders a concrete scoreline as two numbers separated by a dash, not a blank or placeholder score" }
  ]
}
